@model Project.Service.VehicleMake
@{
    ViewBag.Title = "Details";
}

<h2>Make details</h2>

<table class="table">

    <tr>
        <th>@Html.DisplayNameFor(Model => Model.VehicleMakeId)</th>
        <th>@Html.DisplayNameFor(Model => Model.Name)</th>
        <th>@Html.DisplayNameFor(Model => Model.Abrv)</th>
        <th></th>
    </tr>

    <tr>
    <tr>
        <td>@Model.VehicleMakeId.ToString()</td>
        <td>@Model.Name</td>
        <td>@Model.Abrv</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = Model.VehicleMakeId }) |
            @Html.ActionLink("Delete", "Delete", new { id = Model.VehicleMakeId })
        </td>
    </tr>

</table>

<br /><br />

@Html.ActionLink("Back", "Index")